# 地形内容分发

SCNET 地形包含两类不同的数据：

- 权威内容：由持久化数据或世界生成产生的单元格值和柱数据；
- 客户端派生数据：光照、切片哈希、CPU 几何体和 GPU 缓冲区。

两者不能共享隐式生命周期。区块具有碰撞数据并不代表已经可以渲染；区块可以渲染，也不代表
更新的权威响应可以覆盖其当前分配。

## 运行时边界

权威运行时通过 `AuthoritativeChunkGenerationPipeline` 恢复或生成内容。
`TerrainChunkContentAuthority` 发布不可变快照。`ServerChunkDistributionScheduler` 负责请求去重、
编码背压、缓存复用和发送。完成的编码任务必须返回调度器，不能绕过其背压机制直接发送。

对于每个客户端，缓存快照的优先级综合考虑当前位置距离，以及根据近期移动计算出的未来 0.75 秒
中心位置预测。这样既能优先发送移动方向上的区块，也不会让玩家身后较近的区块长期饥饿。发送量
同时受 `ServerChunkCountSendPer` 和 `ServerChunkBytesSendPerSecond` 限制，后者默认是每秒
128 KiB。即使单个快照超过字节预算，也允许发送一个，避免异常大的区块永远无法获得发送机会。

编码后的区块被拆分为 900 字节的应用层分片。每个分片都能装入一个 `Unreliable`
`TerrainFragment` 数据报，因此 LiteNetLib 不需要为地形维护大型、不透明的可靠分片消息。
客户端根据分配身份和内容版本进行重组，接受乱序和重复分片，并在常规的五秒区块请求重试之间
保留已经收到的部分。

重试时，如果客户端已经拥有部分分片，就发送有界的缺片位图；服务端只从编码快照缓存中补发
位图指定的分片。每个区块使用一条控制消息发送位图，其大小不能超过协议规定的最大分片数。
系统不使用固定的近距离重复发送，因此恢复流量只用于仍然缺失的分片。坐标被重新分配时，旧
`AllocationGeneration` 的部分传输会被丢弃。只有完整解码的快照才能进入客户端安装队列。
已完成的传输身份会保存在有界集合中，防止迟到的重复分片重新创建组装任务。

远程副本使用具有容量上限的分配滞回策略：区块在 `ContentDistance` 内分配并请求，但已经分配
的区块只有离开 `ContentDistance + 32` 格后才满足空间淘汰条件。活动内容范围之外最多保留
256 个区块，超过容量时优先淘汰最久未进入活动范围的区块。这样既能让已加载内容和几何体在
短距离高速往返时继续复用，也不会让缓冲区随可视距离无限增长。

保留环中尚未完成的区块继续保存其部分分片，但在重新进入活动内容范围之前不会继续发起网络
请求，因此历史缓存不会与当前可游玩区域争夺带宽。

远程客户端通过 `NetworkChunkContentTransport` 接收快照。网络包处理器只将快照分片、版本化
单元格增量和失败结果加入队列，不直接修改地形数组或地形状态。地形更新循环持有更新令牌时
排空传输队列，并通过 `ClientChunkContentCoordinator` 安装快照。
`ClientTerrainDeltaCoordinator` 根据已安装的快照版本对暂存的单元格增量排序，然后再应用。

`InProcessChunkContentTransport` 为同进程内的本地权威端与客户端组合实现相同的请求和快照
契约。它会主动复制快照数组，防止权威存储与渲染存储意外共享可变所有权。
`LocalChunkContentHost` 负责该传输背后的权威区块分配和生成，并以增量、公平调度的方式推进。

`TerrainCellAuthority` 是权威单元格读写的唯一目标。保存操作遍历同一份权威地形；持久化背压
拒绝接收快照时，权威区块不能被释放。地形更新和渲染从组合完成的服务中显式选择 `Authority`
或 `Replica` 内容角色，不再根据全局进程 `WorkType` 推断内容所有权。本地运行时同时构造两份
存储，每帧先推进进程内权威主机，再更新渲染副本。

进程内投递按区块坐标合并。释放权威区块分配时，也会取消尚未安装到副本的快照。
`AllocationGeneration` 仍然是最终的陈旧响应保护，但普通的本地移动不能仅仅因为玩家快速跨越
分配边界就不断积累过期快照副本。

## 身份与版本不变量

每个已分配坐标都有单调递增的 `AllocationGeneration`。请求和响应都携带
`(coords, allocationGeneration)`；属于旧分配的响应会被忽略。

每个权威快照都有 `ContentVersion`。客户端只会为当前分配安装更新的版本。安装内容时必须持有
地形更新令牌，并将客户端派生生命周期重置为 `InvalidLight`。

每次权威单元格修改都会发布可靠有序的 `TerrainCellDelta`，其中携带
`BaseContentVersion` 和 `ResultContentVersion`，结果必须恰好是下一个版本。只有客户端当前
安装版本与基线版本一致时，增量才会被应用。

增量早于对应快照到达时会被暂存，并在快照安装后回放；快照版本已经达到或超过增量结果版本时，
该增量视为陈旧数据。任何版本断层都会使副本失效，并复用常规完整快照请求路径恢复。完整快照与
增量修改因此共享同一条内容版本序列，不再作为相互独立的同步路径。

本地组合遵循相同的版本规则。权威单元格写入会更新本地副本，并通过增量协调器推进其已安装版本；
远程对等端则通过可靠有序控制通道接收同一个增量。

客户端几何体携带 `ClientGeometryContentVersion`。只有该版本与已安装的
`NetworkContentVersion` 一致，并且 GPU 上传完成时，几何体才允许绘制。这样可以防止旧的
后台任务在新快照到达后暴露陈旧几何体。

## 派生数据双缓冲

权威内容发生变化时，旧几何体版本立即失效。新内容生成并上传匹配版本的几何体之前，不绘制旧
内容的几何体。

光照或相邻区块到达也可能使派生数据失效，但这种情况并未改变权威内容。此时已经上传的当前几何体
可以继续绘制，直到替代的光照和几何体构建完成。上传替代几何体时只释放旧 GPU 缓冲区，不能清除
内容版本标记；设备重置和权威内容安装则必须清除该标记。

这种区分可以同时避免以下两种错误：

- 显示基于旧权威快照生成的几何体；
- 仅仅因为当前内容正在重新计算光照就制造可见空洞。

## 线程所有权

主线程通过 `TerrainChunkStateExchange` 请求状态降级，地形工作线程通过它发布状态推进。该类型
使用两个原子单向邮箱。待处理的降级请求会丢弃较旧的工作线程发布结果；重复降级始终保留最低的
目标状态。

`TerrainChunk.MainThreadState` 由游戏逻辑和渲染使用；`WorkerState` 属于地形工作线程或隔离的
权威生成管线。它们的 setter 仅在仓库程序集内部可见。跨线程修改必须经过
`TerrainChunkStateExchange`；安装快照时只有在持有更新器令牌的情况下才能同时重置两种状态。

禁止使用旧的递归相邻区块推进方式。光照依赖由调度器选择，并且每次只推进一个区块的一个状态。

## 必需验证

修改上述边界时需要执行：

1. 聚焦的地形分发和地形更新器测试；
2. 完整的 `Survivalcraft.Test` 测试套件；
3. Linux 构建；
4. 修改几何生成、上传、渲染或传输安装时，执行 GUI 多人联机检查；
5. 修改请求、响应、重试或收敛行为时，执行固定种子的弱网恢复测试。

可重复实验和证据收集方法参见 [NetworkTesting.md](NetworkTesting.md) 和仓库中的网络稳定性
skill。
